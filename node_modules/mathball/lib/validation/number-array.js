'use strict';

/*
 * Number Array Validation
 */

module.exports = function (arg, func) {
	var flag = 0;
	if (arg && arg.constructor == Array) arg.forEach(function (item) {
		if (typeof item != 'number' || item + 1 == item || item != item) flag = 1;
	});
	if (!arg || arg.constructor != Array || flag) throw new TypeError('Invalid argument received: ' + JSON.stringify(arg) + '\n\'' + func + '()\' only accept an array of real numbers!\n');
};